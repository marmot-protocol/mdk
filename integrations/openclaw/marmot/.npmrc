# Use the locally-installed pnpm; do not auto-switch to the `packageManager`
# version pinned by transitive dependencies (the openclaw package pins its own
# pnpm, which would otherwise hijack `pnpm` invocations in this package).
manage-package-manager-versions=false
